<h2 class="text-2xl font-semibold text-center text-gray-800 mb-6">Cache Manager</h2>
<div class="max-w-lg mx-auto my-8 p-6 bg-white border border-gray-300 rounded-lg shadow-lg">
  <div data-controller="address-autocomplete" >
    
    <div>
      <p><strong>Number of keys:</strong> <%= @cache_keys.count %></p>
      <p><strong>Last cache time:</strong> <%= @last_cache_time %></p>
    </div>
    <form action="<%= admin_clear_cache_path %>" method="post">
      <input type="hidden" name="_method" value="delete">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 mt-4 rounded" onclick="return confirm('Are you sure you want to clear the cache?');">
        Expire Cache
      </button>
    </form>
  </div>
</div>
